# OpenAI API Blueprint Configuration for GZM
# Environment configuration for the local OpenAI-compatible API server

# Environment (development, test, staging, production)
ENVIRONMENT=development

# Authentication - API keys for accessing the service
# In development mode, these will be auto-generated if not provided
API_AUTH_TOKENS=gzm-dev-key-12345678901234567890,test-key-12345678901234567890

# Server Configuration
HOST=127.0.0.1
PORT=8001
LOG_LEVEL=INFO

# CORS Settings (comma-separated domains, optional)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:8000

# Rate Limiting (requests per minute)
RATE_LIMIT_PER_MINUTE=60

# GZM-specific settings (these should match your main .env)
# GEMINI_API_KEY=your-gemini-api-key
# GEMINI_MODEL=gemini-1.5-flash
# LLM_TEMPERATURE=0.0
# LLM_MAX_TOKENS=2048